# Frontend em produção no Render usa proxy /api → backend (render.yaml).
# Não é necessário configurar variáveis para o deploy padrão.
#
# Se hospedar o frontend em outro lugar (ex.: Vercel), defina:
# VITE_API_URL=https://analytic-pulse-api.onrender.com
