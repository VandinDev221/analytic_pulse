-- Fase: Synthetic Monitoring (Playwright / browser)
-- check_type 'browser' usa a coluna existente monitors.check_type (VARCHAR).
-- keyword = seletor CSS opcional que deve ficar visível na página.
-- url = página a abrir (http/https).

-- Sem ALTER obrigatório; este arquivo documenta o contrato para deploys.
SELECT 1;
